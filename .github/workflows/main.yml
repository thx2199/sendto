name: morning
on:
  schedule:
  - cron:  '0 22 * * *'
  workflow_dispatch:

jobs:
  send_message:
    runs-on: ubuntu-latest
    name: send to your friend

    steps:
    - name: checkout
      uses: actions/checkout@v3
      with:
        ref: ${{ env.GITHUB_REF_NAME }}

    - name: sender
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'
        architecture: 'x64'
    - run: pip install -r ./requirements.txt && python ./main.py
